#include "matrix.h"
#include <stdio.h>


int main() {
    // Matrix myMatrix, myMatrix2, myMatrix3;
    int nRows, nCols;
    
    // printf("Masukkan nRows 1: ");
    // scanf("%d", &nRows);
    // printf("Masukkan nCols 1: ");
    // scanf("%d", &nCols);
    // createMatrix(nRows, nRows, &myMatrix);
    // printf("Masukkan elemen sebanyak %d baris dan %d kolom: \n", nRows, nCols);
    // readMatrix(&myMatrix, nRows, nCols);

    // printf("Masukkan nRows 2: ");
    // scanf("%d", &nRows);
    // printf("Masukkan nCols 2: ");
    // scanf("%d", &nCols);
    // createMatrix(nRows, nRows, &myMatrix2);
    // printf("Masukkan elemen sebanyak %d baris dan %d kolom: \n", nRows, nCols);
    // readMatrix(&myMatrix2, nRows, nCols);

    // printf("myMatrix: \n");
    // displayMatrix(myMatrix);
    // printf("\n");

    // printf("myMatrix2: \n");
    // displayMatrix(myMatrix2);
    // printf("\n");

    nRows++;
    printf("%d\n", nRows);

    return 0;
}
